.comments-header
  %h4
    = commentable.comment_threads.count == 0 ? "no" : commentable.comment_threads.count
    comments
= render partial: "comments/form", locals: {new_comment: new_comment} if signed_in?
.comments-container
  = render partial: "comments/reply", locals: {comments: commentable.root_comments}
